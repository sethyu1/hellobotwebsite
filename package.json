{
  "name": "hellobot-website",
  "private": true,
  "version": "1.0.0",
  "description": "HelloBot — static React site (esbuild bundle, no in-browser Babel).",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "serve": "python -m http.server 8765"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "chokidar": "^3.6.0"
  }
}
